import React from 'react';
import './style.css';

const Header = () => (
    <header id="main-header">
        <div className="home">
            <a href="/">Página principal</a>
        </div>
    </header>

);
// <Link to={`Main`}>
export default Header;